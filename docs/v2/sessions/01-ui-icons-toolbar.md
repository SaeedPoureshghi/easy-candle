# Session v2-01 — Icon toolbar + UI polish

**Phase:** 1  
**Branch:** `session/v2-01-ui-icons-toolbar`  
**Depends on:** v1 complete (sessions 01–08 `done`)

## Goal

Professional denser chrome with icon controls, plus `docs/v2/` scaffolding and root progress pointing at v2.

## Scope (do)

- Add `lucide-react`
- Shared `IconButton` for toolbar actions
- Icon-first ReplayControls, selectors, start picker, AppShell header
- Create `docs/v2/` session docs; update root `PROGRESS.md` / `SESSIONS.md`

## Out of scope

- Spacebar hotkey (v2-02)
- Indicators, drawings, paper trading

## Exit criteria

- [x] Toolbar uses icons with `title` / `aria-label`
- [x] `docs/v2/` tree exists; root progress tracks v2-01 as `implemented`
